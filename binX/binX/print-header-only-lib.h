#ifndef printlib_header_only_header
#define printlib_header_only_header

#include <cstdio>

void print_header_only(char const* message){
    printf("From the header only library: %s\n", message);
}

#endif
